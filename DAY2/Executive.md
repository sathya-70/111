Your access is safe and your data is safe; no data was lost. A Windows 11 upgrade removed the old remote-access client, and the automatic install of the new client did not run because a detection check was missing. We removed leftover old VPN registry entries, forced an Intune sync, deployed the new client, applied split-tunnel settings, and confirmed connectivity to all internal network segments. No action is needed.
