No data was lost, and access has been restored: after the Windows 11 upgrade, the old remote-access app was removed and the new app was not automatically reinstalled because a device check was missing. We cleared leftover old VPN registry entries, forced an Intune sync, deployed the new client, applied split-tunnel settings, and confirmed connection to all internal network areas. If you see the same issue, contact the DWP Service Desk.
